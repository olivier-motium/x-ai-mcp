# x-ai-mcp environment variables for devbox deployment
# Copy to /home/ubuntu/x-ai-mcp/.env and fill in values.

# Transport — use streamable-http for devbox
MCP_TRANSPORT=streamable-http
MCP_HOST=0.0.0.0
MCP_PORT=8420

# X API credentials
X_BEARER_TOKEN=
X_CLIENT_ID=c1ZoVThEZVR6RTBMS2t2RDJCeWo6MTpjaQ
X_CLIENT_SECRET=
X_USER_ID=

# OAuth token path (relative to working directory)
X_TOKEN_PATH=./token.json

# xAI / Grok API key (optional, for intelligence tools)
XAI_API_KEY=
